import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    flex: 0,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 20,
    width: 40,
    height: 40,
    paddingRight: 5
  }
});